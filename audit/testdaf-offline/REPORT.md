# Offline Digital TestDaF implementation checkpoints

No live qualification, provider calls, media generation, inventory writes, or release flags are authorized in this phase.

## Foundation
Saved profile ID is authoritative in backend and frontend. Unknown saved IDs fail closed in the backend; legacy resolution only applies without a saved ID. Profile edits preserve the explicit variant when the legacy target is unchanged. 23 backend and 32 frontend tests passed. Commit 46d4e860.

## T1 checkpoint
Reusable speech-act matching, category assignment, grouped candidate selection, and lexical cloze now have generator contracts, deterministic validators, independent source-only audit contracts, bounded source-preserving repair, practice grading, and production reading renderer dispatch. Offline fixtures deliberately test mechanics, not language quality. Test results: 69 new backend cases, 58 profile/manifest/MC regressions, 6 browser/MC checks; frontend typecheck passed.

All seven reading parts remain disabled. Existing reading MC retained. No official scale conversion.

### Paused tasks ? official information required
- lesen_2: current itemCount=4. Official demo page 6 presents five movable paragraphs. Need official explanation of how these yield four scored items before implementing raw grading. Do not assume fixed first paragraph or adjacent-pair scoring.
- lesen_7: current constraints contain only itemCount=3. Official demo pages 14?15 require text AND graphic; profile lacks that source requirement. Requested confirmation before adding it. Error candidates must not be confused with three erroneous sentences.

Source: https://www.testdaf.de/fileadmin/testdaf/downloads/Demo_Version_digitaler_TestDaF/Beispielaufgaben_Demo-Version_digitaler_TestDaF.pdf

All editable exam facts remain in german_exams/testdaf_digital.py. Fixture copies are test data, not runtime configuration.

## T2 checkpoint
All seven listening interaction contracts implemented: overview short answers, grouped notes, summary sentence errors, video speaker categories, video outline notes, multiple choice, and aligned spoken/displayed words. Script generation is independent of media delivery; generator-supplied assets are rejected. Explicit answer variants and profile normalization only, with no semantic-equivalence claim. Source-only audits require coverage and reject ambiguity, unsupported keys, outside knowledge and implausible distractors.

A shared task workspace now dispatches renderers from manifest task types and rejects stale/mismatched envelopes. Media teardown aborts listeners, pauses playback and clears URLs. Summary reveal-after-media is profile-driven. Video uses a video element, never audio substitution. 100 backend cases and 7 mocked browser media cases passed; existing 8 workspace tests passed; frontend typecheck passed.

Release blockers: all parts need live linguistic qualification. All listening parts need playable, script-aligned media delivery. Video acquisition/generation is not implemented. Short-answer variants are practice scoring only, not a replacement for official human assessment. No paid media calls occurred.

## T3 checkpoint
Both writing task types have content generation/validation, editor, word guidance/count, timer, local draft recovery, submission lifecycle and injectable practice-feedback contracts. Structured graphics render accessible tables. Grader output rejects invented official scores, wrong dimension coverage, wrong word counts and fabricated learner quotes. 26 backend tests and two browser tests passed. Draft identity includes account, profile/version, part and generation identity.

Remaining implementation: connect the injected productive grader to the authenticated production grading service. Current default fails visibly and preserves the draft; no fake success or fake grading. This is a release blocker in addition to live qualification.

## T4 checkpoint
All seven speaking task types (spoken_advice, spoken_option_comparison, spoken_text_summary, spoken_information_comparison, recorded_topic_presentation, spoken_argument_response, spoken_measure_critique) reuse the same generic productive-task contract as writing: `generate_productive`/`grading_request`/`grade_productive`/`validate_feedback` in `german_exam_productive.py` now branch on `SPEAKING_TYPES` for recording-duration validation (bounded by `speakingSeconds`, rejects non-finite/negative/over-limit/NaN) and evidence timestamps (`startSeconds` must fall inside the submitted recording) instead of word counts and text quotes. `german_exam_speaking.py` dispatches TestDaF's seven task types straight to `generate_productive`, leaving the existing TELC/Goethe `sprechen_1`/`sprechen_2` part_id-based generator untouched.

One generic speaking renderer (`frontend/js/features/german-exam/speaking-task.ts`, `mountSpeaking`) drives all seven from part constraints: microphone permission request, optional preparation countdown (source hidden during preparation when `hideSourceAfterPreparation`), recording with elapsed/remaining countdown, automatic stop at `speakingSeconds` when `recordingPolicy.autoStop`, manual stop only when `manualStopAllowed`, audio preview, retry gated by `retryAllowed`, upload, submission with injectable grader, and failure handling that preserves the recording for retry instead of discarding it. All dependencies (getUserMedia, MediaRecorder, upload, grader, clock) are injectable for tests; no real speech/model call is made. `spoken_information_comparison` and `spoken_argument_response` require a `script` (spoken) source; since no TTS audio exists in this offline phase, their fixtures correctly leave `media.audioUrl` unset, and the renderer disables recording with a visible "Source audio unavailable" status rather than allowing a task with no audible material.

TestDaF-specific speaking facts (speakingSeconds, preparationSeconds, hideSourceAfterPreparation, requiredSourceKinds, sourcePages, recordingPolicy) remain centralized in `testdaf_digital.py`; `recordingPolicy` is injected once by the shared `_part()` helper for every speaking part (practice policy, not an official exam rule — noted in a source comment). `task-workspace.ts` registers the seven speaking task types against `mountSpeaking` through the same manifest task_type -> renderer registry as writing/media/selection types; no new page/component was built per task type.

33 backend tests (`test_german_exam_productive.py`, including the new speaking-duration/evidence/generation/dispatch cases across all 7 speaking parts) and 8 browser tests (`speaking-task-browser.spec.mjs`: recording lifecycle incl. preparation countdown, auto-stop, manual-stop gating, retry, upload/grading failure recovery, teardown, and the missing-policy guard) passed. Regenerated `tests/e2e/fixtures/german-exam-manifests.json` from the updated profile (manifest fixture is generated, not hand-edited). Frontend typecheck passed; no regression in TELC/Goethe speaking or writing suites.

All seven speaking parts remain `available=False`. Release blockers: no real transcription/grading yet (contracts and fixtures only); no real speech-source audio pipeline for the two script-based comparison tasks (audio generation is out of scope for this phase); live linguistic qualification still required for every part.

Preparation-phase sequencing for `spoken_information_comparison` (two-source comparison with no stated preparationSeconds in the cited demo) and the exact multi-phase structure implied by some tasks (e.g. listen-then-respond ordering) are represented as a single recording pass per the existing shared productive-task/recording contract; if the official demo specifies a distinct multi-phase interaction (e.g. separate listen phase before a timed response), that would need official confirmation before changing the shared contract — not assumed here.

## T5 checkpoint
New shared engine module `german_exams/result.py` implements module-separated results with three explicitly distinct kinds, so a profile without a published raw-to-scaled conversion can never be shown an invented official score: `practice_raw_result` (plain item correctness, always available), `official_style_scaled_result` (points/maxPoints/optional pass, computed only from a caller-supplied verified `ScoringSpec` via the existing `raw_to_points`/`is_pass`, returns `None` otherwise), and `practice_formative_result` (writing/speaking task coverage — a count, never a score). `build_module_result()` assembles exactly one module's result from these and raises if given neither objective nor productive data; there is no function anywhere in the module that accepts more than one module's data (asserted by introspecting every function's parameters in a dedicated test), so a combined exam-wide PASS/FAIL is structurally impossible, not just avoided by convention.

Because every TestDaF `PartBlueprint.scoring` is `None` (no published raw-to-scaled conversion — see testdaf_digital.py's own `rawToScaledConversionAvailable: False`), `official_style_scaled_result(None, ...)` returns `None` automatically for TestDaF once its objective parts are eventually released, with no TestDaF-specific check anywhere in this module — the gate is the presence of a verified `ScoringSpec`, exactly the same mechanism TELC/Goethe already use. No TDN classification function exists: TDN bands describe official 0-20 scaled-score ranges (see `TDN_BANDS`/`SCORING_METADATA` in testdaf_digital.py, unchanged, still descriptive-only), but the raw-item-count -> scaled-score conversion is not published anywhere verified, so no function here computes or labels one — implementing it would require the same official source testdaf_digital.py's docstring already flags as missing.

Mirrored on the frontend as `frontend/js/features/german-exam/module-result.ts`: `validateModuleResult` rejects a result missing both `objective`/`productive`, an `official` result with no backing `objective` result, an impossible correct/total or partsCompleted/totalParts pair, and (defensively) any `tdn`/`scaledScore`/`rawScore` key on the official result or `score`/`points`/`tdn`/`scaledScore` key on the productive result. `renderModuleResult` shows raw correctness (with percent when available), the official-style score only when present, an explicit "No official scaled score is available for this exam yet" note when a module has objective results but no verified scoring, and formative coverage for writing/speaking — never a numeric score. `renderExamResultSummary` renders one independent `<section>` per module and never renders a combined verdict (asserted directly: no "overall"/"final result"/"pass/fail" text anywhere in its output).

14 backend tests (`test_german_exam_result.py`) and 6 browser tests (`module-result-browser.spec.mjs`) passed. Frontend typecheck passed; full german_exam* + german_learner_profile + german_practice backend suite (692 cases) passed, no regression.

Release blockers: this is result *representation* only — nothing here yet reads real attempt data from `german_exam_attempts`/writes a module summary automatically; a future wiring step (T6's exam-session final summary, or a dedicated results endpoint) must call `build_module_result`/`renderModuleResult` with real aggregated counts. No TestDaF part is scored by this or any other change in this phase.

## T6 checkpoint
Full-exam-simulation architecture, generic across every profile. Backend: a new `DeliveryPolicy` dataclass (`fixedTaskOrder`, `backNavigationAllowed`, `additionalUnscoredTrialTasks`) on `shared.py`, an optional `delivery_policy` field on `ExamProfile` (None = today's unchanged free-navigation/no-timer behaviour), and `manifest.py` now serialises it generically as `deliveryPolicy`. `TESTDAF_DIGITAL.delivery_policy` is wired from the existing `DELIVERY_METADATA` dict in testdaf_digital.py (previously computed but unused — now actually consumed), so the single-file-of-facts rule holds: `DELIVERY_METADATA` is still the only place these three TestDaF facts are edited. TELC/Goethe are untouched and their manifests now correctly serialise `deliveryPolicy: null` (asserted directly against `GERMAN_EXAM_PROFILES`, not a TestDaF-specific check).

Frontend: `frontend/js/features/german-exam/exam-session.ts` (`ExamSession`) is a pure state-machine class — no DOM, no owned timer/interval — driven entirely by `manifest.deliveryPolicy` and an injectable `now()`/`Storage`, so it is trivially unit-tested with a fake clock. It implements: per-part `submissionState` (not_started/in_progress/submitted/expired) and answer tracking; `canNavigateTo`/`goToPart` enforcing `fixedTaskOrder` (must submit the current part before advancing, one step at a time) and `backNavigationAllowed` (a completed part becomes unreachable when false); a per-module countdown (`remainingModuleSeconds`) that starts the first time a module is entered and never resets; `tick()`, called by the caller's own timer, which auto-locks a module whose deadline has passed — marking every not-yet-submitted part `expired` and auto-advancing into the next module; `hasUnsavedRisk()` for refresh/close protection (true only while the current part is `in_progress`, e.g. an active recording, not after submission — autosave already covers everything else); JSON snapshot autosave to an injected `Storage`, scoped by `profileId:profileVersion` so a structural change never resumes into a stale part index, and a corrupted snapshot falls back to a fresh session instead of throwing; and `finalSummary()`, which reports only the modules actually completed and labels `additionalUnscoredTrialTasksInOfficialExam` explicitly rather than ever fabricating extra trial tasks — there is no function anywhere in this class that combines more than one exam session, matching T5's module-separation rule.

2 new backend tests in `test_german_exam_profile_testdaf_digital.py` (`test_delivery_policy_is_wired_from_delivery_metadata_and_exposed_on_the_manifest`, `test_a_profile_without_a_delivery_policy_gets_a_null_manifest_field_not_a_default_guess`, file now 30 cases total) and 12 frontend tests (`exam-session.test.mjs`, fake-clock-driven: fixed-order gating, back-navigation gating, free-navigation fallback, module-timer expiry/auto-advance, a module with no duration never expiring, autosave/resume, corrupted-snapshot recovery, profile-version-scoped recovery, unsaved-risk detection, finish-and-lock-out-further-navigation) passed. Full german_exam*/german_learner_profile/german_practice backend suite (694 cases) and full frontend unit suite (804 cases, only the 2 pre-existing unrelated TS-registry-mirror failures from before this branch) passed. Frontend typecheck passed.

Release blockers: `ExamSession` is not yet wired into `exam-workspace.ts`'s free-practice UI (that UI intentionally stays free-navigation/untimed — a full-exam simulation is a distinct mode, not built here) or rendered against `module-result.ts`'s summary components; both are natural next steps once real generation is live. No TestDaF part is enabled by this change; `additionalUnscoredTrialTasks=True` remains a label only — no trial tasks are generated or simulated.

## T7 checkpoint
Audited `german_exam_inventory.py` and its migration (`20260920_000001_german_exam_inventory.sql`) for TELC-specific assumptions per the roadmap. Found none: `is_stocked()`/`take()`/`replenish_async()`/`generate_and_store()` never branch on a profile id; the RPC (`german_exam_inventory_take`) and both tables are already scoped by `profile_id` + `profile_version` + `module` + `part_id` (the RPC's own WHERE clauses filter on all four), and `STOCKED_MODULES`/the migration's `module` check constraint already include `speaking` generically (TestDaF's own live-only speaking module needs no change — it already falls outside `STOCKED_MODULES` the same way TELC/Goethe speaking does, with no profile check anywhere). No code changes were required — this checkpoint is tests only, proving what the architecture audit found rather than refactoring anything.

Added `test_german_exam_inventory_testdaf.py` (5 tests, fake inventory rows/fake Supabase double only — no real database, no live generation, no TestDaF item is ever inserted): the RPC call for a TestDaF part is scoped to `p_profile_id="testdaf_digital"` with TestDaF's own `profile_version`; two `take()` calls for the same module/part_id spelling but different profiles (`testdaf_digital` vs `telc_c1_hochschule`) produce two RPC calls with two distinct `p_profile_id` values (the isolating key, asserted directly); `is_stocked()` applies the exact same generic rule to TestDaF as every other profile (reading/listening/writing stockable, speaking not, same as TELC/Goethe); `take()` alone cannot seed anything (`generate_and_store` monkeypatched to raise if ever called); and — reusing the real profile registry, not a stub — every TestDaF part across all four modules still 501s via `generate_task()` before reaching `is_stocked`/inventory/dispatch, guarding against a future accidental single-part `available=True` flip silently starting to serve or generate stock for it.

No TestDaF inventory row was inserted anywhere in this phase (real or fake-DB persisted) — every test here uses an in-memory fake Supabase double. 5 new backend tests passed; full german_exam*/german_learner_profile/german_practice suite (699 cases) passed, no regression.

## T8 checkpoint
Neither existing German Exam QA script (`scripts/qa_reading_mc.py`, `scripts/verify_german_exam_live.py`) enforces a cost/sample budget before calling a provider — both make live calls bounded only by an operator-supplied `--samples`/`--topics` count, with no `--max-cost-usd`, no pre-call budget check, and no override of the usage-meter's default (module-derived) feature label. Per the roadmap's own instruction to check for an existing bounded pattern first: there wasn't one, so this checkpoint builds it, shared rather than duplicated per script.

New `scripts/qa_budget.py` (generic — usable by any German Exam QA script, not TestDaF-specific): `QaBudget` refuses to construct without both `max_cost_usd` and `max_samples` explicitly given (`ValueError`, no defaults — an unbounded run cannot start), and refuses values above the hard safe ceiling (`SAFE_MAX_COST_USD = 0.25`, `SAFE_MAX_SAMPLES = 1`). `guard_new_sample()` must be called immediately before starting the next sample's paid call; it raises `BudgetExceeded` once either bound is already spent — enforced going INTO the next call, never discovered only by totalling cost afterward. `add_budget_args()` adds `--max-cost-usd`/`--max-samples` as `required=True` CLI arguments (omitting either is an argparse error, not a silent default). `labeled_usage(label)` is a context manager that overrides `app.services.llm_json.record_usage`'s feature label for every call made inside it, so a QA run's `usage_events` rows are attributed to an explicit label instead of `__main__` or the generator module's own name (llm_json's default `_caller_feature()` behaviour), and restores whatever hook was installed before it on exit (including on exception).

New `scripts/qa_testdaf_live.py`: one parametrized script (`--module {reading,listening,writing,speaking}`) rather than four near-duplicate files — every module's generator (`generate_reading_part`/`generate_listening_part`/`generate_writing_part`/`generate_speaking_part`) shares the identical `(profile, part, plan, topic) -> (content, meta)` signature, so one dispatch table covers all four. Usage labels are `testdaf_lesen_qa` / `testdaf_hoeren_qa` / `testdaf_schreiben_qa` / `testdaf_sprechen_qa` per module, applied via `labeled_usage()` for the whole run. Each sample writes its own diagnostics JSON (content, validation metadata, `gen_timing` diagnostics, wall time, `partAvailable` — always `False`, proving the script never changes it) plus a run summary with `releaseGatePassed: false` always set (human inspection and cost/latency acceptance remain required — this script never self-certifies a release). `run()` calls `budget.guard_new_sample()` before every sample and stops immediately (writing a `terminated` record, non-zero-safe exit) once the budget is spent, both before the loop's next iteration and again defensively after each completed sample.

Backend/python-ai/scripts is now a regular package (`scripts/__init__.py` added) purely so `from scripts.qa_budget import ...` resolves deterministically under pytest — `tests/scripts/` (an unrelated existing package for `eval_retrieval.py`) would otherwise be found first by Python's import system regardless of `sys.path` order, since a namespace-package portion never outranks a regular package found on any other `sys.path` entry. Confirmed this doesn't affect real invocation (`python -m scripts.qa_reading_mc --help` / `python -m scripts.qa_testdaf_live --help` both still work) and doesn't change existing script behavior.

11 new backend tests (`test_qa_budget.py`) covering: refusal to construct without both bounds, refusal above the safe ceilings, refusal of negative cost, requiring a non-empty label, accepting the safe-default bounds exactly, `guard_new_sample()` stopping before both the sample-count and cost caps (the cost case proven as a pre-next-call check, not a post-hoc report), an unpriced call counted as zero cost (never a free pass), CLI args being required with no default, and `labeled_usage` overriding/restoring the usage hook correctly including across an exception. 6 new backend tests (`test_qa_testdaf_live.py`, every generator call/`gen_timing` call/`.env` load monkeypatched — zero real provider calls) covering: all four modules have distinct `testdaf_*_qa` labels and none is `__main__`; the dispatch table resolves the real generator functions; a single bounded sample writes exactly one record and a summary with `releaseGatePassed: false`; a generator exception is recorded as a failed sample rather than raised/crashing the run; and `--max-samples=1` permits exactly one generation call, never more.

This phase performed **zero** live calls with this harness — it was written and unit-tested only, exactly as the roadmap requires. Full backend suite (all 2377 tests across the whole python-ai codebase, not just German Exam) passed after adding `scripts/__init__.py`, confirming no regression anywhere else.

## Audit-and-fix pass (Phases 1-5, no live QA execution)

**Phase 1 (audit checklist).** Reviewed T0-T8 commits, `testdaf_digital.py`, the QA harness, and the T1-T8 REPORT.md entries above against the roadmap. Confirmed as already true and unchanged: 23 task types registered, all parts `available=False`, 699 German-exam tests / 2377 full-suite tests passing, zero live calls made anywhere to date. Confirmed as genuinely open (not fixable offline): every part still needs live linguistic qualification; Hören needs real playable media (TTS live-status unconfirmed, video pipeline nonexistent); Schreiben/Sprechen need a confirmed-reachable production grading/transcription service; sprechen_4/sprechen_6's exact phase structure was unconfirmed.

**Phase 2 (official-source verification).** Re-fetched the same official demo PDF already cited in `OFFICIAL_SOURCES["reading_mc_example"]` (`Beispielaufgaben_Demo-Version_digitaler_TestDaF.pdf`) and extracted its embedded text directly (`pdftotext`, not OCR/guessing — the file has genuine selectable text, including a full printed solution key ("Lösungen") for every reading task and timed-phase headers for every speaking task).

- **lesen_2** (paused, itemCount=4): the solution key prints all five numbered paragraphs `[1]`-`[5]` as the answer to "Textabschnitte ordnen." Corrected to `itemCount=5`. `MODULE_METADATA["reading"]["itemCount"]` updated 34 -> 35 to stay consistent (no other official aggregate reading item-count table exists in the PDF's extractable text — its "Prüfungsteil Lesen" overview table's Items column is image-rendered, not selectable text — so the per-task solution-key counts are the only extractable authoritative figures, and they now sum to 35).
- **lesen_7** (flagged, itemCount=3, no requiredSourceKinds): the solution key confirms itemCount=3 was already correct ("Es gibt genau drei inhaltlich falsche Sätze"). The task instructions state "Beachten Sie auch die Informationen aus der Grafik" and the demo shows a bar chart beside the text — `requiredSourceKinds` was missing this entirely (not merely under-specified). Added `requiredSourceKinds=("text","graphic")`.
- **Sprechen phase structure**: all seven task types' timed-phase headers were extracted. sprechen_1/2/3/5/7 match the current single preparation-then-recording model exactly (prep/speak seconds line up with the demo's printed timers). sprechen_4 and sprechen_6 do not: the demo prints *additional* timed phases before preparation — sprechen_4 shows ~30s graphic view + ~20s audio playback + 01:30 prep + 01:30 speak (preparationSeconds was missing entirely from the profile — no prep phase existed at all); sprechen_6 shows ~58s audio playback + 01:30 prep + 02:00 speak (prep/speak were already correct). Corrected sprechen_4's `preparationSeconds` to 90 and added `sourcePlaybackSecondsApprox` (documentation field only, not consumed by any renderer) to both. The generic `mountSpeaking` renderer only implements preparation -> recording; it does **not** implement a source-viewing/listening phase before preparation. This is now a confirmed, sourced, real gap — not a guess — and is documented as a release blocker for exactly these two parts in `LIVE_QUALIFICATION_PLAN.md`, not built here (multi-phase rendering is a real feature addition, not a smallest-necessary profile fix, and the roadmap explicitly reserves shared-architecture changes for when the behavior is proven shared — it's shared only across these two TestDaF parts so far, not proven needed by TELC/Goethe).

All four corrections are additive/corrective data changes in `testdaf_digital.py` only; no reading/speaking generator or validator code was touched (none exists yet for the affected reading task types; the speaking dispatch/renderer code path is unchanged). `tests/e2e/fixtures/german-exam-manifests.json` regenerated via the existing `UPDATE_MANIFEST_FIXTURE=1 pytest` mechanism (never hand-edited). 699 german_exam*/german_learner_profile/german_practice tests and the full 2379-test backend suite (2377 baseline + 2 new dry-run tests from the Phase 3 work below) pass with no regression, and no TELC/Goethe profile file was touched.

No authoritative source was found (or searched further, per the roadmap's bounded-research instruction) for a reading-module aggregate official item-count table beyond what's in this one demo PDF — testdaf.de's structure/scoring pages (already in `OFFICIAL_SOURCES`) describe format and scoring philosophy, not exact per-task item counts.

**Phase 3 (QA-harness audit).** Read `qa_testdaf_live.py` and `qa_budget.py` end to end (no execution against a real provider). Confirmed present and correct: `QaBudget` refuses to construct without both `--max-cost-usd`/`--max-samples`, refuses values above the safe ceiling ($0.25 / 1 sample), `guard_new_sample()` is called immediately before each sample and raises before the next paid call (not just totalled afterward — also re-checked defensively after each completed sample), a generator exception is recorded as a failed sample rather than crashing the run, the script never writes to inventory or flips `available`, and every record/summary reports task type, sample number, cost, validation result (semantic verification is already embedded inside each module's own `generate_*_part` pipeline per T1-T4, so the QA record's `validation` field already carries the semantic-QA outcome — no separate step was missing), failure reason, and total spend.

Two genuine gaps found and fixed:
1. **No dry-run mode existed.** Added `--dry-run` to `qa_testdaf_live.py`: runs the real `main()`/`run()` path (real argparse, real budget accounting, real sample loop, real termination logic) against a new `_dry_run_generator` fixture that makes zero provider/network calls and skips `load_dotenv`. Two new tests (`test_dry_run_never_imports_or_dispatches_to_a_real_generator`, `test_dry_run_still_stops_before_a_further_sample_once_the_budget_is_spent`) prove `_generator()` (which imports a real, network-capable module) is never resolved in dry-run mode, and that the cost-exhausted-after-one-sample termination path still fires correctly through the real CLI.
2. **A real, collection-order-dependent test-isolation bug** in `test_qa_budget.py`/`test_qa_testdaf_live.py`: both files unconditionally delete and reimport `scripts.*` from `sys.modules` at file-load time (worked around a `tests/scripts/` package-shadowing issue from T8). Run together, whichever file pytest collects *second* rebinds `sys.modules["scripts.qa_budget"]` to a new module object, while the *other* file's already-imported `BudgetExceeded`/`QaBudget` names keep pointing at the stale object — so a `pytest.raises(BudgetExceeded)` in the first file can fail to catch the (different-identity) exception a fresh import in that same test raises. Reproduced directly (`pytest tests/test_qa_testdaf_live.py tests/test_qa_budget.py` failed; each file alone passed) and fixed by guarding the reimport so it only happens once per session regardless of collection order; verified fixed in both file orderings and against the full 2379-test suite.

Zero real provider calls were made anywhere in this phase (all tests use monkeypatched generators / a fixture generator).

**Phase 4 (dependency gap analysis, no new implementations built).** Lesen: text-only generation, no external dependency beyond the existing LLM pipeline; lesen_7's chart needs the same "structured graphics render accessible tables" component T3 already built for schreiben_2 — needs confirming it covers a read-only reading-module graphic, not just writing's editable data table. Hören: a real, generic audio path already exists (`app/services/tts_provider.py`'s `TTSProvider` abstraction, proxied to the Qwen3-TTS backend from the separate Hören/Qwen-TTS initiative) — TestDaF listening should call this exact service once its voice is confirmed live, not build a new one; **video has no implementation anywhere in this codebase** (TELC/Goethe have no video-listening task type either) — a genuine, unresolved blocker for hoeren_4/hoeren_5 specifically, not a QA-harness or profile-data problem. Schreiben: T3's existing release blocker (injected grader not yet connected to "the authenticated production grading service") is unchanged and applies identically to TestDaF via the same profile-agnostic `grade_productive(..., grader=...)` contract already used by TELC/Goethe writing — a config/wiring question, not new code, once that service is confirmed live for any profile. Sprechen: `app/services/german_exam_speaking_practice.py` already has a working `transcribe()` function (`gpt-4o-mini-transcribe`) used elsewhere in the engine — TestDaF speaking grading should wire through this via the same injectable-grader contract, not a new integration; sprechen_4/sprechen_6 additionally carry the Phase 2 phase-structure gap and the same Qwen3-TTS dependency as Hören for their "script" source kind.

**Phase 5 (qualification plan).** Wrote `audit/testdaf-offline/LIVE_QUALIFICATION_PLAN.md`: a bounded matrix for all 23 parts (grouped Lesen/Hören/Schreiben/Sprechen) with samples-needed, max-provider-calls, automated/human checks, and pass/fail criteria per part, plus an explicit call-out that this repo has **no existing written policy** on how many successful live samples are sufficient before flipping a part's `available` flag — that needs an explicit decision before Phase 6, not an assumption. The plan marks hoeren_4/hoeren_5 (video) and sprechen_4/sprechen_6 (renderer phase gap) as currently blocked ahead of any live QA, independent of cost/budget concerns.

No inventory rows were written, no `available` flag was changed, and no real OpenAI/TTS/video/provider call was made anywhere in this pass.
